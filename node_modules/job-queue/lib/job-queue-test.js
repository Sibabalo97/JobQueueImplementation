// Generated by CoffeeScript 1.7.1
(function() {
  var JobQueue, assert, vows;

  JobQueue = require("./job-queue");

  vows = require("vows");

  assert = require("assert");

  assert.withinBounds = function(value, min, max) {
    return assert((min <= value && value <= max), "expected within [" + min + " .. " + max + "], got " + value);
  };

  vows.describe("job-queue").addBatch({
    "With no consumers or jobs": {
      topic: new JobQueue,
      "No consumers": function(jobQueue) {
        return assert.deepEqual(jobQueue.consumers, []);
      },
      "No pending jobs": function(jobQueue) {
        return assert.equal(jobQueue.pendingJobs, 0);
      }
    },
    "With MovingWindowRateLimitedConsumer and 500 jobs": {
      topic: function() {
        var jobId, jobQueue, _fn, _i;
        jobQueue = new JobQueue([1, 2, 3, 4, 5].map(function(consumerId) {
          return new JobQueue.MovingWindowRateLimitedConsumer((function(job) {
            return job.process(consumerId);
          }), 5, 300);
        }));
        jobQueue.addConsumers([6, 7, 8, 9, 10].map(function(consumerId) {
          return new JobQueue.MovingWindowRateLimitedConsumer((function(job) {
            return job.process(consumerId);
          }), 8, 600);
        }));
        _fn = function(jobId) {
          return jobQueue.enqueue({
            id: jobId,
            process: function(consumerId) {}
          });
        };
        for (jobId = _i = 1; _i <= 500; jobId = ++_i) {
          _fn(jobId);
        }
        return jobQueue;
      },
      "initially": {
        topic: function(jobQueue) {
          return jobQueue;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "500 pending jobs": function(jobQueue) {
          return assert.equal(jobQueue.pendingJobs, 500);
        }
      },
      "after 30 ms": {
        topic: function(jobQueue) {
          setTimeout((function(_this) {
            return function() {
              return _this.callback(null, jobQueue);
            };
          })(this), 30);
          return void 0;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "435 pending jobs": function(jobQueue) {
          return assert.equal(jobQueue.pendingJobs, 435);
        }
      },
      "after 330 ms": {
        topic: function(jobQueue) {
          setTimeout((function(_this) {
            return function() {
              return _this.callback(null, jobQueue);
            };
          })(this), 330);
          return void 0;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "410 pending jobs": function(jobQueue) {
          return assert.equal(jobQueue.pendingJobs, 410);
        }
      },
      "after 480 ms": {
        topic: function(jobQueue) {
          setTimeout((function(_this) {
            return function() {
              return _this.callback(null, jobQueue);
            };
          })(this), 480);
          return void 0;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "410 pending jobs": function(jobQueue) {
          return assert.equal(jobQueue.pendingJobs, 410);
        }
      },
      "after 630 ms": {
        topic: function(jobQueue) {
          setTimeout((function(_this) {
            return function() {
              return _this.callback(null, jobQueue);
            };
          })(this), 630);
          return void 0;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "345 pending jobs": function(jobQueue) {
          return assert.equal(jobQueue.pendingJobs, 345);
        }
      },
      "after 1530 ms": {
        topic: function(jobQueue) {
          setTimeout((function(_this) {
            return function() {
              return _this.callback(null, jobQueue);
            };
          })(this), 1530);
          return void 0;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "230 pending jobs": function(jobQueue) {
          return assert.equal(jobQueue.pendingJobs, 230);
        }
      },
      "after 3030 ms": {
        topic: function(jobQueue) {
          setTimeout((function(_this) {
            return function() {
              return _this.callback(null, jobQueue);
            };
          })(this), 3030);
          return void 0;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "0 pending jobs": function(jobQueue) {
          return assert.equal(jobQueue.pendingJobs, 0);
        }
      }
    },
    "With CustomRateLimitedConsumer and 500 jobs": {
      topic: function() {
        var jobId, jobQueue, _fn, _i;
        jobQueue = new JobQueue([1, 2, 3, 4, 5].map(function(consumerId) {
          var counter;
          counter = 1;
          return (function(func, args, ctor) {
            ctor.prototype = func.prototype;
            var child = new ctor, result = func.apply(child, args);
            return Object(result) === result ? result : child;
          })(JobQueue.CustomRateLimitedConsumer, [
            function(job) {
              return job.process(consumerId);
            }, function() {
              var d;
              d = new Date;
              d.setUTCMilliseconds(d.getUTCMilliseconds() + (counter++) * 4.5);
              return d;
            }
          ], function(){});
        }));
        jobQueue.addConsumers([6, 7, 8, 9, 10].map(function(consumerId) {
          var counter;
          counter = 1;
          return (function(func, args, ctor) {
            ctor.prototype = func.prototype;
            var child = new ctor, result = func.apply(child, args);
            return Object(result) === result ? result : child;
          })(JobQueue.CustomRateLimitedConsumer, [
            function(job) {
              return job.process(consumerId);
            }, function() {
              var d;
              d = new Date;
              d.setUTCMilliseconds(d.getUTCMilliseconds() + (counter++) * 7.5);
              return d;
            }
          ], function(){});
        }));
        _fn = function(jobId) {
          return jobQueue.enqueue({
            id: jobId,
            process: function(consumerId) {}
          });
        };
        for (jobId = _i = 1; _i <= 500; jobId = ++_i) {
          _fn(jobId);
        }
        return jobQueue;
      },
      "initially": {
        topic: function(jobQueue) {
          return jobQueue;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "500 pending jobs": function(jobQueue) {
          return assert.equal(jobQueue.pendingJobs, 500);
        }
      },
      "after 30 ms": {
        topic: function(jobQueue) {
          setTimeout((function(_this) {
            return function() {
              return _this.callback(null, jobQueue);
            };
          })(this), 30);
          return void 0;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "480 ± 25 pending jobs": function(jobQueue) {
          return assert.withinBounds(jobQueue.pendingJobs, 480 - 25, 480 + 25);
        }
      },
      "after 330 ms": {
        topic: function(jobQueue) {
          setTimeout((function(_this) {
            return function() {
              return _this.callback(null, jobQueue);
            };
          })(this), 330);
          return void 0;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "410 ± 25 pending jobs": function(jobQueue) {
          return assert.withinBounds(jobQueue.pendingJobs, 410 - 25, 410 + 25);
        }
      },
      "after 480 ms": {
        topic: function(jobQueue) {
          setTimeout((function(_this) {
            return function() {
              return _this.callback(null, jobQueue);
            };
          })(this), 480);
          return void 0;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "380 ± 25 pending jobs": function(jobQueue) {
          return assert.withinBounds(jobQueue.pendingJobs, 380 - 25, 380 + 25);
        }
      },
      "after 630 ms": {
        topic: function(jobQueue) {
          setTimeout((function(_this) {
            return function() {
              return _this.callback(null, jobQueue);
            };
          })(this), 630);
          return void 0;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "350 ± 25 pending jobs": function(jobQueue) {
          return assert.withinBounds(jobQueue.pendingJobs, 350 - 25, 350 + 25);
        }
      },
      "after 1530 ms": {
        topic: function(jobQueue) {
          setTimeout((function(_this) {
            return function() {
              return _this.callback(null, jobQueue);
            };
          })(this), 1530);
          return void 0;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "150 ± 25 pending jobs": function(jobQueue) {
          return assert.withinBounds(jobQueue.pendingJobs, 150 - 25, 150 + 25);
        }
      },
      "after 3030 ms": {
        topic: function(jobQueue) {
          setTimeout((function(_this) {
            return function() {
              return _this.callback(null, jobQueue);
            };
          })(this), 3030);
          return void 0;
        },
        "10 consumers": function(jobQueue) {
          return assert.equal(jobQueue.consumers.length, 10);
        },
        "0 pending jobs": function(jobQueue) {
          return assert.equal(jobQueue.pendingJobs, 0);
        }
      }
    }
  })["export"](module);

  process.on("uncaughtException", function(err) {
    console.error("Caught exception: " + err);
    return process.removeAllListeners("uncaughtException");
  });

}).call(this);
